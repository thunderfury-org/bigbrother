pan115:
  request_interval_ms: 1500

pan123:
  # 第三方续期 API 地址（OpenList 提供，用于 OpenAPI token 续期）
  api_address: "https://api.oplist.org/123cloud/renewapi"
  refresh_token: ""

pan1:
  # 123分享社区（pan1.me）登录 Cookie，用于搜索帖子并在导入时解锁隐藏分享链接
  base_url: "https://pan1.me"
  cookie: ""
  reply_message: "感谢分享,太棒了！"

pan189:
  # 天翼云盘账号密码；用于自动获取 PC 客户端 session
  username: ""
  password: ""

tmdb:
  api_key: ""

# OpenAI-compatible API for LLM title extraction（可选；配置后用于正则解析失败时的标题提取）
openai:
  api_key: ""
  base_url: "" # 例如 https://api.openai.com/v1
  model: ""    # 例如 gpt-4o-mini

telegram:
  bot_token: ""
  user_id:

library:
  remote_path: ""
  local_path: ""

emby_proxy:
  enable: false
  host: 0.0.0.0
  port: 8097
  upstream_base_url: http://127.0.0.1:8096
  api_key: ""
